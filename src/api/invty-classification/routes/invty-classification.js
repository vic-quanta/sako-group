'use strict';

/**
 * invty-classification router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::invty-classification.invty-classification');
