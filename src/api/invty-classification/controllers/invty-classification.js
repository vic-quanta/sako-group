'use strict';

/**
 * invty-classification controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::invty-classification.invty-classification');
