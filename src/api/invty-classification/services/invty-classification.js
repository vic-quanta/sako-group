'use strict';

/**
 * invty-classification service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::invty-classification.invty-classification');
